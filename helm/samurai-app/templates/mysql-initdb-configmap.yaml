apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-initdb
  labels:
    app: samurai-app
data:
  01-init.sql: |
    {{ .Files.Get "initdb/init.sql" | nindent 4 }}
  02-seed.sql: |
    {{ .Files.Get "initdb/seed.sql" | nindent 4 }}
